这个工具用来记录最近的N条指令的PC寄存器和其他寄存器的信息。

默认记录200条，如果需要改变这个记录大小，修改代码中的Q_SIZE即可。

另外有个问题是，FCEUX的emu.paused函数无法正确执行（推测模拟器暂停时，lua脚本也暂停了），所以我无法找到一个合适的外部事件时机来输出记录，
所以代码中注册了一个写内存事件来作为输出时机，因此使用者需要修改此事件注册的内存地址来自定义输出时机。